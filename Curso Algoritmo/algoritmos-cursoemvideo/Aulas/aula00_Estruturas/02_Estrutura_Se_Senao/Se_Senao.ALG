algoritmo "Se_Senao"
var

inicio
    // Se o bloco1 for verdadeiro, o bloco2 não será executado.
    se (Verdadeiro) entao 
        bloco A
    // Se o blocoA for falso, o blocoB será executado.
    senao
        bloco B
fimalgoritmo