algoritmo "Se_Senao"
var

inicio
    // Se o bloco A for verdadeiro, o bloco B não será executado.
    se (Verdadeiro) entao 
        bloco A
    // Se o bloco A for falso, o bloco B será executado.
    senao
        bloco B
fimalgoritmo