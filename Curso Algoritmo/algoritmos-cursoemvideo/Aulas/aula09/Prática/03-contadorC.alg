algoritmo "Contador"
var
    cont, n: inteiro
inicio
    cont <- 0
    escreva("Informe até onde irei contar: ")
    leia(n)

    enquanto (cont < n + 1) faca 
        escreval(cont)
        cont <- cont + 1
    fimenquanto
    escreval("Contagem finalizada!")
fimalgoritmo